# Database used by sqlx compile-time query checks (cargo build).
# Create it with: make db-setup
DATABASE_URL=sqlite:deploykeys.db

# Logging level: trace, debug, info, warn, error
RUST_LOG=info

# GitHub App client ID for the OAuth device flow (optional override).
# DEPLOYKEYS_GITHUB_CLIENT_ID=Iv1.xxxxxxxxxxxxxxxx
